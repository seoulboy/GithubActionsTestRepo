name: build_and_test

on: [push, pull_request]

jobs:
  ios:
    name: ios
    runs-on: macOS-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Build
        run: swift build -v
      - name: Test
        run: swift test -v -c release
